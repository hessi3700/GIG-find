# Copy to apps/api/.env (and optionally apps/web/.env)

# === apps/api (.env) ===
# Database: path to SQLite file (default: ./data/gigfind.db)
# DATABASE_PATH=./data/gigfind.db

# JWT secret: used to sign login tokens. Generate a random key (see README).
# Example (run in terminal to generate):
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
#   openssl rand -base64 32
# JWT_SECRET=your-generated-secret-at-least-32-chars

# PORT=3000

# === apps/web (.env) ===
# VITE_API_URL=http://localhost:3000
