name = "gig-find-api"
main = "src/worker.ts"
compatibility_date = "2025-09-01"
compatibility_flags = ["nodejs_compat", "enable_nodejs_http_server_modules"]

# Secrets: set via `wrangler secret put MONGODB_URI` and `wrangler secret put JWT_SECRET`
# For local dev use .dev.vars (not committed):
# MONGODB_URI=...
# JWT_SECRET=...

[vars]
# Optional: add non-secret env here
